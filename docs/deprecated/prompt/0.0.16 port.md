# 

```
现在以CGC当前的config顺序，更新CGC Config Addon，要求：
- 顺序保持跟CGC一致
- CGC已经deprecated的不要再加入，相应的翻译键也删掉
- CGC Config Addon里的翻译键取CGC目录./docs/wiki/configuration/下相应的中英文翻译，目前只有中文的键名是wiki没有的，英文键名就是驼峰式拆开
- 注意CGC更新了部分配置项的默认值，CGC Config Addon也需要调整
- 还需要做格式调整，setTooltip写在entryBuilder下一行
- 目前ClothSyncConfig作为修改好的示范，涉及deprecated配置项移除，新增HideEntityHitbox配置项，setTooltip格式调整

代码范围：
- CGC的config在dev.xcolorful.customgun.core.config和dev.xcolorful.customgun.client.config包下
- CGC Config Addon需要修改dev.xcolorful.cgcconfig.cloth.config下三个子包里的config类
```

# 

I'll start by exploring both codebases to understand the current state.

Now let me read the CGC source configs and the Cloth addon configs in parallel.

Now the Cloth addon configs, starting with the reference model ClothSyncConfig.

Now let me look at the wiki docs and the addon's language files.

Now let me check the registration/screen files and the tag classes to see the full picture.

I have the full picture. Two decisions need your input before I edit.

Decisions noted. Let me set up tasks and start editing.

Now the client configs.

Now the lang files.

Now let me verify the JSON is valid and every referenced translation key exists.

Let me check the line endings and the unexpected file in the diff.

All files pass IDEA inspections. Now a semantic check: entry order and default values against CGC.

The ClothSyncConfig mismatch is my extractor mis-parsing sub-category blocks. Let me fix it and re-verify.

All 54 entries match CGC order and defaults. Final format check.

One wiki inconsistency found. Let me clean up my temp scripts and review the final state.

The repo convention is no trailing newline; my rewrites added one. Let me fix that.

CGC Config Addon 已按 CGC 当前 config 更新完毕，9 个 config 类全部改完，未提交。

**顺序与默认值**（脚本核对：54 个配置项的顺序 + 默认值全部与 CGC 一致）

- 重排：`ClothAmmoConfig`（穿透/破坏玻璃/速度系数提到最前）、`ClothRenderConfig`（按 CGC init 顺序）
- 默认值调整（CGC 改过、addon 没跟上的）：
    - 枪械开火声音距离 64 → 320，消音 16 → 256
    - 全局子弹速度系数 2.0 → 1.0
    - 长按瞄准 / 长按趴下 true → false
    - 禁用枪械侧倾 false → true，强制显示枪械HUD false → true

**deprecated 移除 + 翻译键删除**

- Other：`TargetSoundDistance`
- Resource：`EnableLazyClientAssetLoad`
- Render：`HitMarkerStartPosition`、`HeadShotDebugHitbox`、`KillAmountEnable`、`KillAmountDurationSecond`、`TargetRenderDistance`、`AutoSelectGunSmithTableFilter`、`DamageCounterResetTime`、`BlockEntityTranslucent`
- Sync：`ClientGunPackDownloadUrls`（你之前的 diff 只删了 `EnableTableFilter` 的键，这个漏了，我一并删掉）

**格式**：全部 54 个 entry 的 `.setTooltip` 都提到 `entryBuilder.startXxx` 的下一行，含参考文件里剩下没统一的 4 个（`ammo_box_stack_size` / `enable_prone` / `server_shoot_network_v` / `server_shoot_cooldown_v`）。

**其他**

- `ClothResourceConfig` 类保留，只留空的 `resource` 分类（按你说的，不删类文件）
- 英文键名修正一处：`enable_first_person_bullet_tracer` 的显示名 `First person bullet tracer enable` → `Enable first person bullet tracer`

**校验**：zh_cn / en_us 各 126 键、键集完全一致、JSON 可解析、所有 Java 引用的键都存在，无孤儿键（只剩 `key.*` 两个非 config 键）；9 个文件 IDEA 检查零报错；顺手把 Write 引入的行尾（LF、末尾换行）对齐回仓库原有约定，diff 保持最小。

**一处需要你决定**：wiki 的 `Ammo config.md` 里 `GlobalBulletSpeedModifier` 的英文描述被断行弄坏了——"modifier, the initial speed of the bullet will be multiplied by this value" 跑到了上面 `Path: \`ammo\`` 那行，导致该项描述只剩 "Global bullet speed"。addon 现有值是完整的那句，我按原样保留了，没动 wiki（那是 CGC 仓库、不在本次范围）。要我一并修 wiki 吗？

工作区里还有你自己的待处理改动（`gradle.properties`、`docs/deprecated/prompt/0.0.16 port.md`）我没有碰。

# 

```
wiki断行我修了
CGC Config Addon里的顺序要跟CGC里IModConfigSpec变量声明的顺序严格一致，并且翻译键里的顺序也要保持一致，我看git diffs里没有新增，你大概是漏了调整顺序了
```

# 

你说得对——Java 里的顺序我对齐了 `init()`，但 lang 文件里每个分类的键顺序还是旧的。让我确认当前状态。

确认了：Java 顺序对，但 `render` 和 `ammo` 两个分类在 lang 文件里的键顺序还是旧顺序。让我按 CGC 声明顺序重排。

Now let me verify both the Java order and the lang order against CGC's declaration order.

抱歉，确实漏了——Java 里我对齐的是 CGC 的 `init()` 调用顺序，但 lang 文件里每个分类的键顺序没跟着调。已修正。

**改动**：`render` 和 `ammo` 两个分类在 `zh_cn.json` / `en_us.json` 里的键顺序重排为 CGC 的 `IModConfigSpec` 声明顺序，每个键连同它的 `.desc` 一起移动：

- `render`：`gun_lod_render_distance` → `disable_gun_tilting` → `enable_first_person_bullet_tracer` → `replace_vanilla_crosshair` → `crosshair_type` → `enable_gun_hud` → `keep_display_gun_hud` → `force_display_gun_hud` → `enable_shooter_operation_hud` → `append_resource_location_in_tooltip` → `enable_laser_fade_out` → `bullet_hole_particle_life` → `bullet_hole_particle_fade_threshold` → `disable_movement_attribute_fov`
- `ammo`：`pass_through_blocks` → `destroy_glass` → `global_bullet_speed_modifier` → `ignite_block` → `ignite_entity` → `explosive_ammo_destroys_block` → `explosive_ammo_fire` → `explosive_ammo_knock_back` → `explosive_ammo_visible_distance`

其余分类（`key` / `sound` / `zoom` / `gun` / `other` / `sync` 三个子段）本来就是对的，未动。文件的分节空行、2 空格缩进、无末尾换行、LF 行尾都保持原样，diff 里只有重排和删除。

**新增的核对**（脚本按 CGC `public static IModConfigSpec<...>` 声明顺序逐项比对，deprecated 已剔除）：

```
java  OK  key/render/resource/sound/zoom/ammo/gun/other/sync   54 entries
lang  OK  zh_cn / en_us 键集与顺序完全一致（126 键）
```

现在 Java entry 顺序、lang 键顺序、CGC 声明顺序三者严格一致。

另外上次提的那个 wiki 问题还在（`Ammo config.md` 里 `GlobalBulletSpeedModifier` 的英文描述被断行拆到了 `Path: \`ammo\`` 那行），需要我改 wiki 的话说一声。
